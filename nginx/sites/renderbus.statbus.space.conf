server {
    listen 80;
    server_name renderbus.statbus.space;

    root /var/www/html/renderbus.statbus.space/public;
    index index.html;

    access_log off;
    log_not_found off;
    error_log /var/log/nginx/renderbus.statbus.space.log;
}